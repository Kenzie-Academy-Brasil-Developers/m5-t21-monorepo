// TODO: Implementar os testes quando chegar a parte de Paginação
describe("INTEGRATION: GET /api/members", () => {
  test("should return an empty list when no members are available", () => {
    expect(1).toBe(1);
  });
});
